services:
  request-design-api:
    build: .
    container_name: request-design-api
    ports:
      - "3000:3000"
    mem_limit: 2g
    memswap_limit: 2g
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - LLM_BASE_URL=${LLM_BASE_URL}
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL}
      - BROWSER_ENGINE=playwright
      - MAX_CONCURRENT_JOBS=${MAX_CONCURRENT_JOBS:-1}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s