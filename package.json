{
  "name": "request-design-runtime",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=.env src/index.ts",
    "start": "bun --env-file=.env src/index.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.6.0",
    "openai": "^4.77.0",
    "playwright": "^1.61.1",
    "postgres": "^3.4.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
